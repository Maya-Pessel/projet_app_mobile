import * as React from 'react';
import { Button, Center, Text } from "native-base";

const Loading = () => {
    return (
        <Center h="full">
            <Text>Loading...</Text>
        </Center>
    );
}

export default Loading;
