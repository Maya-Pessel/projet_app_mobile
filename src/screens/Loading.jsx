import { Button, Center, Text } from "native-base";
import * as React from 'react';

const Loading = () => {
    return (
        <Center h="full">
            <Text>Loading...</Text>
        </Center>
    );
}
const 

export default Loading;
